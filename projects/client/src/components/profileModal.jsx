import Modal from './Modal';
export default function UploadProfilePict({isOpen, onClose}) {

    return(
        <>
        <Modal isOpen={isOpen} onClose={onClose}>
            <div className='p-6'>
                <div>
                    
                </div>
            </div>
        </Modal>
        
        </>
    )
}